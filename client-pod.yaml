apiVersion: v1
kind: Pod
metadata:
    name: client-pod
    labels:
        component: web
spec:
    containers:
        - name: client
          image: gaurav1990/multi-loginfe
          ports:
            - containerPort: 3000